module.exports.getRating = (req, res) => {
  /**
   * if @params = lessonId then retrieve lesson rating from db
   * if @params = userId then retrieve user rating from db 
   */
}

module.exports.addRating = (req, res) => {
  /**
   * if @params = lessonId then add lesson rating to db
   * if @params = userId then add user rating to db 
   */
}

module.exports.updateRating = (req, res) => {
  /**
   * if @params = lessonId then update lesson rating in db
   * if @params = userId then update user rating in db 
   */
}
